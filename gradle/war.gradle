apply plugin: "war"

bootWar {
    mainClass = "com.bs23.simpleecommerce.SimpleEcommerceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
